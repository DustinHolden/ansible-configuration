# inventory.yml
all:
  hosts:
    mac-mini-m2-pro:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3

      # ansible_host: mac-mini-m2-pro.mudpuppy-walleye.ts.net
      # ansible_host: mac-mini-m2-pro
      # ansible_user: dustinholden
      # ansible_ssh_private_key_file: ~/.ssh/id_rsa_dotfiles
      # ansible_python_interpreter: /usr/bin/python3
      